# Prompt 7: Kiểm tra Package Prisma và Đồng bộ Lock file

## Yêu cầu
- Kiểm tra lại các package Prisma sau khi hạ cấp vì vẫn gặp lỗi build.
- Xử lý lỗi `npm ci` không khớp giữa `package.json` và `package-lock.json`.
